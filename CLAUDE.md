# daily-app — 作業ルール

小さいアプリを1日1つ作って溜め、**1つずつ整備していく**リポジトリ。
手順の詳細は `README.md` にある。ここには常に守るルールだけを書く。

## 大前提

- **ビルドツールを持ち込まない。** `docs/` はブラウザで開けば動く素の HTML/CSS/JS だけ。
  `package.json` / バンドラ / フレームワークを足さない。
- **1アプリ＝1ディレクトリ。** `docs/apps/<slug>/` の中で完結させる。
  アプリ間で import し合わない。
- **共通は `docs/shared/` の3ファイルだけ。**
  新しい共通ファイルを増やす前に、本当に2つ以上のアプリで要るか確かめる。
- **全体リファクタをしない。** 既存アプリのコードを作り直さず、
  指定された1アプリだけを触る。「ついでに」他を直さない。

## 色・余白・文字サイズ

`docs/shared/tokens.css` の CSS 変数だけを使う。各アプリに生の色コード（`#3b6cf6` など）や
生の px を書かない。必要な値が無ければ、まず tokens.css に足してから使う。

## アプリを足す

`README.md` の「アプリを1つ足す」に従う。`docs/apps.json` への追記を忘れない。
追記しないと一覧に出ない。

## 整備状況（`apps.json` の `status`）

| 値 | 意味 |
|---|---|
| `draft` | 動くが、見た目・文言・操作はまだ整えていない |
| `polished` | `README.md` の整備基準をすべて満たした |
| `template` | `_template` のみ |

**`polished` に上げるのは、整備基準を実際に確認してから。** 見ないで上げない。

## ローカルの既存アプリを取り込むとき

形式が混在している前提で、**まず分類してから動かす**。いきなり移さない。

1. 各アプリを次の3つに分ける
   - **静的**（HTML/CSS/JS のみ、ブラウザで開けば動く）→ `docs/apps/<slug>/` にそのまま入る
   - **ビルド要**（React / Vite など）→ **この時点では移さない。** 置き場所を利用者に確認する
   - **サーバ要**（Flask / Streamlit など）→ **GitHub Pages では公開できない。** 別扱いにする
2. 分類結果を利用者に見せて、移す対象を確認してもらう
3. 承認された「静的」だけを取り込む

取り込みのときの約束。

- **元のファイルは消さない。コピーする。** 移動ではなくコピー。
- **コードの中身を書き換えない。** まず動く状態で `draft` として置く。
  トークンへの寄せや UI の整備は、そのあと1つずつ別の作業としてやる。
- パスが壊れていないか、実際にブラウザ（またはヘッドレス）で開いて確認してから登録する。

## API キー・秘密情報

このリポジトリは Public で、`docs/` は GitHub Pages で全世界に公開される。
**キー・トークン・個人情報をコードにもログにも書かない。**
それらが要るアプリは、ここに置かず利用者に相談する。

## 表記

学生団体の表記は **kokokara**（cocokara ではない）。
